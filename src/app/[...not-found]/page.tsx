import NotFound from "@/views/NotFound";

const NotFoundPage = () => {
  return (
    <div className="flex flex-col min-h-screen items-center justify-center px-6 py-24 sm:py-32 lg:px-8">
      <NotFound />
    </div>
  );
};

export default NotFoundPage;
